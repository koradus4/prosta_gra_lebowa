services:
  - type: web
    name: prosta-gra-lebowa
    runtime: python
    plan: free
    autoDeploy: true
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn -w 1 -b 0.0.0.0:$PORT backend.app:create_app()
  healthCheckPath: /healthz
    envVars:
      - key: PYTHON_VERSION
        value: 3.11
